# MHC Demo – optional environment variables
# Copy to .env and adjust as needed (e.g. cp .env.example .env)

# Port (default: 3000 in dev, 5398 in production)
# PORT=5398

# Override Python used for MSA (run_clustal.py). Leave unset to use .venv or system python3.
# PYTHON_MSA_PATH=/usr/bin/python3
